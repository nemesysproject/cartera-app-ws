# cartera-app-ws

Proyecto Node.js API web usando TypeScript.

## Scripts
- `npm run build`: Compila TypeScript a JavaScript
- `npm run start`: Ejecuta el servidor Express usando ts-node

## Estructura
- `src/`: Código fuente
- `test/`: Pruebas

## Endpoints
- GET `/` — Responde con mensaje de funcionamiento

## Requisitos
- Node.js >= 14

## Instalación
1. Instala dependencias: `npm install`
2. Compila: `npm run build`
3. Ejecuta: `npm run start`
